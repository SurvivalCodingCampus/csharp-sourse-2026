# Day 05 TIL

- 이름: `<진미>`
- 작성일: `<2026-09-07>`

## 1. 오늘 막힌 부분 또는 내린 판단

`<리스트에 뭘>`

## 2. 수정 전과 수정 후

### 수정 전

```csharp
// <9, 10, 11, 16슬라이드로 각각 클래스와 인터페이스 및 메인 작성>
```

### 수정 후

```csharp
// <
각각 매개변수에 대한 생성자를 작성했지만  program.cs에 이미 해당 파일이 작성되어 있었기에 생성자로 인스턴스 한 Name으로 path 정의
동료가 try catch에 각각 return 값이 들어가야 한다고 함
    파일이 없어서 계속 오류 남 > 직접 people.json 파일을 만듬
    >
```

## 3. AI 사용 여부와 채택, 거절한 이유

- AI 사용 여부: `<사용함>`
- 질문: `<List 자료형 작성법>`
- 제안받은 내용: `<.Add 형식>`
- 채택 또는 거절한 내용: `<>`
- 판단한 이유: `<>`


## 4. 검증 결과

- 빌드: `<성공>`
- 실행 결과: `<파일을 만들어 정보를 입력한 내용이 출력됨>`
- 추가로 확인한 내용: `<>`
![img.png](img.png)

## 5. 아직 궁금한 점

`<>`

## 6. 다음에 적용할 것

`<>`

## 7. 기억할 것
`< p => p.Age >= 19를 풀어서 쓰면
    int people(int Age){
    return >= 19;
    }>
<_people에서 '_'를 사용하는 이유 :: 메서드 내부에서 지역 변수, 매개변수(private임을 의미)>
<DataSource:: 데이터는 다양한 형태(JSON, CSV, DB, 클라우드 등)가 존재>
<DataSource종류::
Text (.txt 등)
File (로컬 파일)
JSON (웹 API에서 자주 사용)
XML
CSV (엑셀 같은 형식)
RDBMS (MySQL, PostgreSQL 등 관계형 DB)
NoSQL (MongoDB, Firebase Firestore 등)
등등>
`
