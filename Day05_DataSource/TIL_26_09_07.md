# Day 05 TIL

- 이름: `<김진용>`
- 작성일: `<2026-0907>`

## AI 사용 여부와 채택, 거절한 이유

- AI 사용 여부: `<사용함>`
- 질문: `<테스트방법>`
- 제안받은 내용: `<1. 비동기 메서드 선언: public void METHOD() 대신 public async Task METHOD()로 선언해야 await 비동기 I/O 호출이 끝날 때까지 테스트 러너가 대기합니다.
2.환경 격리 (TestInitialize / TestCleanup): 하드코딩된 "people.json" 대신 시스템 임시 디렉터리(Path.GetTempPath())에 유니크 GUID 파일명을 사용하면, 여러 테스트가 병렬로 돌아도 파일 충돌이나 데이터 오염이 발생하지 않습니다.
3.단언문(Assert): MSTest에서는 Assert.AreEqual(expected, actual), Assert.IsTrue(condition), Assert.IsNotNull(obj) 등의 문법을 사용합니다.>`

AI 대화 전문을 붙이지 말고 질문, 판단, 검증 내용을 요약합니다.

## 검증 결과

- 빌드: `<성공>`
- 실행 결과: 파일이없을때 빈리리스트반환,필터링및삭제 검증,저장후불러오기 데이터일치

## 아직 궁금한 점

아직 테스트를 스스로 짜보는게 어렵다

