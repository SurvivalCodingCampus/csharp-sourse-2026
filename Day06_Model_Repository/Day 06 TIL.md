# Day 06 TIL

- 이름: `<문재원>`
- 작성일: `<2026-09-08>`

## 1. 오늘 막힌 부분 또는 내린 판단

- Interface를 생성자로 주입 시키는 부분 <br>
=> 인터페이스를 구현한 클래스의 인스턴스를 만들어서 생성자에 넣어준다.

## 2. 수정 전과 수정 후


### 수정 전

```
  
```

### 수정 후

```
    private IItemDataSource _itemDataSource;
    
    
    [SetUp]
    public void Setup()
    {
        _itemDataSource = new MockItemDataSource();
    }
```



## 3. AI 사용 여부와 채택, 거절한 이유

- AI 사용 여부: `사용`
- 질문: `인터페이스 생성자 주입 후, 인스턴스 생성`
- 질문2: `Test 진행 시 이미 구현한 인터페이스를 재사용 할 수 있나`
- 제안받은 내용: `인터페이스를 구현한 클래스를 인스턴스 한다`
- 제안받은 내용2: `방법1: 위임 // 방법2: Mock 데이터 방법`
- 채택 또는 거절한 내용: `질문1 채택 // 질문2: 방법1 채택`
- 판단한 이유: `AI가 제안 해준 내용을 직접 해보고 되는 것을 보고 채택` <br>
`위임으로 채택을 하였으나, Mock 방법은 기록해두고 파악해 볼 예정`



## 4. 검증 결과

- 빌드: `<성공>`
- 실행 결과: `<출력값으로 실행결과 확인>`
- 추가로 확인한 내용: ``

## 5. 배운 것

- Interface 생성자 주입 후, 인스턴스 생성 방법 <br>
=> Interface를 구현한 클래스를 인스턴스를 하여 넣어주면 된다.
- 프레임워크별로 Assert 메서드명이 다르다.
- Import에서 하는 using과 class 내부에서 쓰는 using은 다르다. <br>
=> class 안 내부 using은 읽어오고 using 블록문이 끝나면 스스로 닫는다. <br>
=> using 문은 IDisposable 객체를 범위 종료 시 정리한다.


