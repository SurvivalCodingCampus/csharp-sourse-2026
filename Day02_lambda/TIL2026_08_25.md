# Day 01 TIL

- 이름: `김진용`
- 작성일: `2026-08-25`

## 1. 오늘 막힌 부분 또는 내린 판단

`<예상과 달랐던 C# 문법이나 오류를 하나 작성하세요.>`

## 2. 수정 전과 수정 후

### 수정 전

```csharp
// <수정 전 코드를 작성하세요.>
```

### 수정 후

```csharp
// <수정 후 코드를 작성하세요.>
```

`<왜 이렇게 수정했는지 설명하세요.>`

## 3. AI 사용 여부와 채택, 거절한 이유

- AI 사용 여부: `<사용함>`
- 질문: `<나는 C# 에대해 초보자야 LINQ에 대해 알기쉽게 이해하기 쉽게 알려줘>`
- 제안받은 내용: `<
Where(e => 조건): 조건에 맞는 데이터만 걸러냅니다 (필터링).

Select(e => 원하는 항목): 필요한 속성이나 데이터만 쏙 뽑아냅니다 (매핑/추출).

OrderBy(e => 기준): 오름차순(작은 순서대로) 정렬합니다.

Distinct(): 중복된 값을 제거합니다.

Any(e => 조건): 조건을 만족하는 항목이 하나라도 있는지 true/false로 확인합니다.

Max() / Min(): 최댓값 또는 최솟값을 구합니다.>`
- 채택 또는 거절한 내용: `<>`
- 판단한 이유: `<>`

AI 대화 전문을 붙이지 말고 질문, 판단, 검증 내용을 요약합니다.

## 4. 검증 결과

- 빌드: `<성공 / 실패>`
- 실행 결과: `<확인한 동작>`
- 추가로 확인한 내용: `<테스트 또는 예외 상황>`

## 5. 아직 궁금한 점

`<c#에 대해서 좀더 공부해야겠다>`

## 6. 다음에 적용할 것

`<다음 코딩에서 직접 적용할 한 가지>`
